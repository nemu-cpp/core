ishikopath = envvar("ISHIKO_CPP");
import $(ishikopath)/errors/include/Ishiko/Errors.bkl;
import $(ishikopath)/third-party-glue/include/Ishiko/ThirdParty/Boost.bkl;

setting NEMU_CPP_ROOT
{
    help = "Path to the Nemu installation";
    default = '$(NEMU_CPP)/nemu/cpp';
}

template NemuCore : IshikoErrors, Boost
{
    includedirs += $(NEMU_CPP_ROOT)/include;

    if ($(toolset) == gnu)
    {
        libdirs += $(NEMU_CPP_ROOT)/lib;
    }

    if ($(toolset) != gnu)
    {
        libdirs += $(NEMU_CPP_ROOT)/lib/$(arch);
    }
}
